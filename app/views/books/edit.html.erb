<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h2>Editing Book</h2>
      <% if @book.errors.any? %>
        <%= @book.errors.count %>errors prohibited this book from being saved: <br>
        <ul>
    　     <% @book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      <% end %>
      <%= form_with model:@book, url:"/books/#{@book.id}", local:true do |f| %>
        <p>Title</p>
        <%= f.text_field :title %>
        <p></p>
        <p>Opinion</p>
        <%= f.text_area :body %>
        <p>
        <td><%= f.submit 'Update Book', class: 'btn btn-success'%></td>
        </p>
        <!--<div class="form_group">-->
        <!--  <label for="book_title">Title</label>-->
        <!--  <input class="form-control book_title" type="text" name="book[title]" id="book_title" placeholder="Title">-->
        <!--</div>-->
        <!--<div class="form_group">-->
        <!--  <label for="book_opinion">Opinion</label>-->
        <!--  <textarea class="form-control book_body" name="book[body]" id="book_body" placeholder:"Opinion">-->
        <!--</div>-->
        <!--<div>-->
        <!--  <button type="submit" class="btn btn-success mb-2">Update Book</button>-->
        <!--</div>-->
      <% end %>
      <%= link_to "Show", book_path(@book.id) %>
      |
      <%= link_to "Back", books_path %>
    </div>
  </div>
</div>
